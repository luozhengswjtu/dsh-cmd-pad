# dsh 测试命令面板插件 —— 产品设计与功能文档

> 插件名：**cmd-pad**（命令面板）
> 文档版本：**v0.4**（2026-08-23，T07 运行通道落地：新开专用终端 + 终端直写 + 危险双人工确认）
> 适用环境：DeepSeek Harness Web UI（PC 端桌面 Web，≥ 0.1.0-rc.8），Windows

> ⚠️ **现状注记（2026-08-23，落地状态覆盖）**：**「运行」功能已按用户决策恢复并落地（T07，调整记录 #24）**。
> 演进：v0.1 曾实现「运行 = 写入对话输入框」（调整记录 #21 被否决移除）→ T07 恢复为
> **「每次运行新开专用终端 Tab + 终端直写（WS 附加）」**（用户决策），终端直写失败**直接复制**，
> 降级形态（无 better-sidebar）**无运行功能只复制**。本文档与「运行」相关条款（§1.1、§3.4、§4.1、
> §4.2、§4.3、§7 F4/F8/F11）以 T07 落地后的表述为准；`danger` 标记 = 卡片「危险」徽标 +
> 保存时关键词提示勾选 + 运行前确认弹窗（命令原文）+ 终端内不带回车（双人工确认）。

---

## 1. 插件定位

### 1.1 一句话描述

DSH Web UI 里的「测试命令行速查面板」：命令按**自定义分组 + 项目**归档，**一键复制**、主形态**一键运行**（新开专用终端直送执行，见 §4.2），不用再翻笔记、翻聊天记录、翻 `history` 找命令。

### 1.2 解决什么问题

| 现状痛点 | 本插件的做法 |
|---|---|
| 命令散落在笔记、聊天记录、`history` 里 | 集中收纳到面板，按分组归档 |
| 命令又长又容易抄错（参数、路径、IP） | 一键复制，原样进剪贴板 |
| 高频命令每次手动敲 | 主形态：点击「运行」直接送入新开的终端执行；降级形态：一键复制 |
| 换项目后命令对不上号 | 命令可绑定项目分组，面板自动识别当前项目 |
| 团队内命令靠口口相传 | 命令库是一个可手改的 YAML 文件 |

### 1.3 非目标（明确不做）

- 不做命令执行结果的展示与解析（面板只负责"送达"）；
- 不做云端同步、多人协作（命令库落本地文件，分享 = 复制文件）；
- 不做触屏/移动端适配（DSH Web UI 是 PC 端桌面应用）；
- 命令参数占位符（`ping <ip>`）留到后续版本。

---

## 2. 形态与入口（v0.3 重构：双形态，软依赖）

### 2.1 架构决策

cmd-pad 以**软依赖**方式集成 better-sidebar：运行时探测 `ctx.betterSidebar` 服务，**存在则注册为其侧边栏 Tab（主形态），不存在则回退为独立浮动图标 + 抽屉（降级形态）**。一个插件包，两种形态，用户无感切换。

better-sidebar 从 v0.4.0 起是服务化注册表框架：`registerTab` / `registerFileViewer` 扩展点对所有插件开放，其内置的 7 个 Tab（文件/Git/子代理/终端/浏览器等）与三方插件走同一 API，能力对等；生态中已有同构先例（host 半自带 API + client 半注册 Tab 的双面 bundle）。v0.12.0+ 提供 `version` / `features` 能力探测字段，用于新特性的条件启用。

- **依赖声明**：`dsh-better-sidebar` 写入 `peerDependencies` 并标 `optional: true`——装了走 Tab 形态，没装插件照常以抽屉形态加载，安装期只有良性 peer 提示；
- **运行时探测**：`const bs = ctx.get?.('betterSidebar') ?? ctx.betterSidebar`（取得到才注册，取不到静默跳过，走降级分支）；
- **版本门**：`bs.features.includes('badge')` 等能力探测决定角标/生命周期回调是否挂载（v0.12.0+ 才有）。

### 2.2 主形态：better-sidebar Tab

注册描述符（`ctx.effect(() => bs.registerTab({...}))` 包裹，卸载自动撤销）：

| 字段 | 值 | 说明 |
|---|---|---|
| `id` | `cmd-pad:pad` | 包前缀防冲突 |
| `title` | `命令` | 纯文本，无图标emoji |
| `icon` | 单色 SVG（命令列表符号：提示符 `>` + 两行命令线，`stroke: currentColor`） | 唯一保留的图形之一，不携带任何颜色；与内置终端 `>_` 图标明显区分（调整记录 #23） |
| `order` | `45` | 紧跟内置 terminal（40）之后、browser（50）之前 |
| `single` | `true` | 每会话单实例，重复打开聚焦既有 Tab |
| ~~`badge`~~ | ~~命令总数~~ | **已按用户决策移除**（调整记录 #23，2026-08-23）：Tab 不显示命令总数角标 |
| `onActivate` | 切回本 Tab 时拉取最新命令库 | 多标签页/手改 YAML 场景保鲜 |

**此形态下 cmd-pad 不自建任何浮层**：无浮动图标、无抽屉、无遮罩。外壳（面板框架、拖拽分栏、明暗换肤、设置页开关卡片）全部由 better-sidebar 承担，插件只剩纯内容区。Tab 可拖到底部面板、可与其他 Tab 拆分并排，布局按会话持久化。

**项目识别简化**：Tab 组件 props 直接携带 `scope: { sessionId, cwd? }`——better-sidebar 的 Tab 按会话隔离，切会话即用新 scope 重渲染，不再需要插件自己订阅 `ctx.sessions.list`。

### 2.3 降级形态：独立抽屉（无 better-sidebar 时）

沿用 v0.1 的浮动图标 + 右侧抽屉，但按 v0.3 反馈做两处修正：

- **去掉黑色蒙层**：抽屉改为**非模态**——不再罩住页面，不与会话区交互冲突；关闭途径 = Esc / 抽屉顶栏 ✕ / 再次点击浮动图标（点页面其他区域**不收起**，避免复制命令后点输入框时面板消失）；
- **浮动图标让位**：若检测到页面存在 better-sidebar 的角落按钮簇但服务探测失败（极端场景），浮动图标右偏移避让——正常探测路径下二者不会同时出现。

### 2.4 主题与视觉收敛（v0.3 新增硬性规则）

**令牌契约不变**：全面消费 `--dsw-alias-*` 语义令牌（`bg-base` / `bg-layer-1/2` / `border-l2` / `label-primary/secondary` / `state-error-primary` / `bg-mask-1`），每个变量带 `var(--dsw-*, var(--cp-*))` 兜底链；宿主明暗切换、皮肤中心 10 款皮肤换肤自动跟随。

**图标收敛规则（用户定稿）**：

1. **不使用任何彩色图标，不使用 emoji**；
2. 全插件仅保留 **2 处功能必需图形**：better-sidebar Tab 图标（§2.2）、搜索框放大镜——均为 `currentColor` 单色 SVG，颜色完全来自令牌；
3. 语义区分一律用排版承担：分组类型靠**文字前缀**（`项目：`、`上次：`）与**字重/颜色层级**（`label-primary` vs `label-secondary`），不再用 📁📌🕘 等符号；
4. 按钮一律**纯文字**：`复制` / `运行` / `+ 添加`（`+` 是文字字符不是图标）；
5. 状态色仅保留两处文本级用途：危险徽标（`state-error-primary` 文字标签）、强调色（`label-link`，仅激活态与主按钮）。

---

## 3. 分组模型（已定稿，v0.3 仅修订视觉表达）

### 3.1 一个维度：分组

**命令与分组是多对多**，一条命令可属于多个分组。分组只有一类字段——名字（**分组名即主键**，全局唯一，手写 YAML 所见即所得）。分组不是持久实体：分组列表 = 命令库中出现过的所有分组名的聚合，命令删光则分组自动消失。

| 类型 | 判定 | 管理方 |
|---|---|---|
| 自定义分组 | 普通名字（如 `性能采集`、`常用`） | 用户：手动创建、重命名、删除、设常驻 |
| 项目分组 | 名字是工作区绝对路径（`D:\work\car_media`） | 系统：按当前会话 cwd 自动匹配/聚合；用户不能手动增删改 |

### 3.2 项目识别

DSH 每个会话的 header 携带权威 `cwd`：主形态下由 Tab props 的 `scope.cwd` 直接供给；降级形态与 host 半走 `ctx.sessions`（client 侧 `sessions.list.getSnapshot()` 的 `current` / `byId[id].cwd`，host 侧 `sessions.get(id).header.cwd`）。**「当前项目」= 当前会话的 cwd**，插件零猜测直接读。会话未关联工作区时，当前项目视图显示引导空态。

项目显示名取路径末段；多个项目末段重名时带上一级路径消歧（`Temp_Code` → `work / Temp_Code`）。

### 3.3 侧栏分组结构（v0.3 去图标版）

```
┌─ 分组 ──────────────────────────
│ 上次：性能采集            ← 动态 slot（见 3.4），弱化色 + 虚线框
│ 全部
│ 项目：car_media          ← 当前项目（cwd 视图）
│ 常用                     ← 常驻自定义分组（排序靠前 + 常规字重）
│ 性能采集
│ ▸                        ← 更多箭头（仅图标无文字；横向内联展开，▸ 展开 → ◂ 收起，调整记录 #26）
│    其他项目               ← 按项目内最近使用倒序
│      Temp_Code (D:\work)
│    日志抓取               ← 不常驻自定义分组（原「分组」小节标题已移除，调整记录 #26）
```

- **常驻/不常驻**：右键分组项 →「设为常驻 / 取消常驻」（稳定触发，不走悬停）；pinned 名单存 `state.json`；常驻分组无命令时也显示；
- **「常用」**：普通的常驻自定义分组，**没有任何自动逻辑**，用户通过添加表单勾选或编辑把命令收进来；
- **「全部」**：跨分组总览，按分组分节展示；
- **「未分组」**：仅当存在 groups 为空的命令（手改 YAML 可能产生）时出现。

### 3.4 「上次使用」slot

- 指向**视图**（`current-project` 或 `group:<名字>` 都可被指到）；
- 刷新时机：在该视图语境下**复制**命令（运行同样刷新——「运行」恢复后与复制同语义，T07）；添加、搜索态操作、纯浏览**不刷新**；
- 点击 = 跳转到该视图本体；**slot 自身不提供添加入口**（跳转后添加自然归属真实分组）；
- 指向的视图失效（分组删除）时自动隐藏。

### 3.5 添加 / 编辑 / 删除语义（已定稿）

- **添加**：在哪个分组视图点「+ 添加」，分组多选列表中该分组**默认勾选**并提升展示（即使它属于不常驻区）；列表按「自定义分组（常驻在前）→ 项目分组」分区，可再加选任何分组；底部有"输入新分组名"行，保存时自动创建并勾选。在「全部」/搜索态点添加的默认勾选顺序：上次使用的分组 → 当前项目 → 常用。分组超过 8 个时不常驻未勾选项折叠为"显示全部分组 ▸"；
- **删除**：操作名统一为「删除」，语义上下文相关——命令还属于其他分组时 = 仅解除当前分组关联（静默）；当前分组是最后一个归属、或在「全部」/搜索态删除时 = 彻底删除（弹确认，文案明示）；删除后 5 秒内 Toast 可撤销；
- **编辑**：编辑命令本体（标题/命令/备注/危险标记/分组勾选），改动在所有归属分组同步可见；
- **分组删除**（右键）：确认弹窗列出影响——N 条命令解除关联，其中 M 条仅此分组的将彻底删除；可撤销；
- **分组重命名**（右键，仅自定义分组）：级联更新所有命令的归属，分组名冲突时拒绝。

---

## 4. 命令卡片与执行

### 4.1 卡片构成（v0.3 去图标版）

标题（+ 危险徽标：纯文字 pill）→ 命令等宽块（点击即复制，超长两行截断）→ 备注 → 操作行（`复制` 按钮；主形态另有 `运行` 按钮在复制前）。右键卡片 = 运行（主形态）/ 复制 / 编辑 / 删除。

### 4.2 运行通道（T07 定稿：新开专用终端 + 终端直写；降级 = 直接复制）

> ✅ **已落地（T07，2026-08-23，TASK.md 调整记录 #24）**。用户决策定稿：
> ① 运行 = **每次新开专用终端 Tab**（§8 待确认项 2 的备选策略），不复用活跃终端；
> ② 终端直写失败 → **直接复制 + Toast**（写对话输入框方案已被否决，调整记录 #21）；
> ③ 降级形态（无 better-sidebar）**不提供运行入口，只复制**。

**主形态（better-sidebar 在场）**——「运行」新开专用终端并直送命令：

```
运行点击
  → 1. ctx.betterSidebar.openTab({ type: 'terminal' }) 新开专用终端（配额满/禁用/被拒 → 跳降级）
  → 2. 从 getSnapshot() 差集识别新终端 tab id（terminal:<uuid>，遍历 splits + bottomSplits）
  → 3. activateTab 激活新终端（用户可见）
  → 4. 自建一条 WebSocket 附加到该 pty：
       WS /sidebar/ws/terminal?sessionId=<id>&tab=<tabId>&cwd=<cwd>
       onopen 后**等输出流出现提示符 `>`（shell 就绪）**再发送 命令文本 + '\r'
       （PowerShell 冷启动 6–7s，未就绪写入会被吞——实机教训）
  → 5. 发送后**保持连接不 drop**（新终端无 UI 视图长连时，bare drop 会在 reconnect
       grace 30s 到期后杀 pty，长命令/交互命令中断——实机教训）
  → 任一环节失败 → bs.closeTab 回滚新开的 tab（防泄漏）→ **复制 + Toast「已复制，到终端粘贴执行」**
```

协议依据（better-sidebar **v0.13.1 实机实证**，非仅源码）：终端 = xterm.js + node-pty over
`/sidebar/ws/terminal`；**输入帧是原始文本**，控制帧为 JSON（`{type:'resize'|'close'|'park'}`）；
宿主在连接时回放历史输出，**同一 pty 支持"视图长连 + 临时写入连"并存**（双 WS 附加实证 13/13：
B 写入 A 可见、A 写入 B 可见、附加不重建 pty、双工对称、bare drop 走 grace 后 pty 存活）。

**危险命令的终端语义**：`danger: true` 命令经确认弹窗（命令原文 + 取消/确认）后，
**只发送命令文本、不发送 `\r`**——命令停在终端提示符上，由用户在终端里亲眼确认后自行回车
（双人工确认；实机实证：危险命令停在提示符、无执行输出）。

**降级形态（无 better-sidebar）**——**无运行功能**：卡片不渲染「运行」按钮，右键菜单无「运行」项
（用户决策 2026-08-23，调整记录 #24；无侧边栏终端则无运行通道）。

| 形态 | 运行 | 复制 |
|---|---|---|
| 主形态 | 终端直写（新开专用终端）→ 失败复制 | 剪贴板 + Toast |
| 降级形态 | 无（仅复制） | 剪贴板 + Toast |

> 风险提示：终端直写通道是 better-sidebar **未文档化承诺的内部协议**，存在其版本升级后行为变化的可能。
> 本次（T07）已对实际安装 v0.13.1 实机实证（双 WS 附加 + 端到端写文件副作用）；**每次升级 better-sidebar
> 后必须回归验证**（§8 待确认项 1，接入规范 §3）。

### 4.3 安全

- `danger: true`：**确认弹窗（完整命令原文）+ 终端内不带回车**（命令停在提示符，由用户亲眼确认后回车，双人工确认——T07 实机实证）；
- 浏览/搜索/切换分组等任何非点击行为不触发执行（仅「运行」按钮/右键「运行」点击触发）；
- 保存时检测到 `rm/del/format/wipe/reboot` 等关键词可提示勾选 danger（前端提示，不强制）。

### 4.4 键盘

`/` 聚焦搜索；`Esc` 逐层关闭（弹层 → 右键菜单 → 清空搜索 → 降级形态下收起抽屉）；~~↑↓+Enter 运行选中项（P2）~~（暂不启用）。

---

## 5. 数据模型与存储（不变）

### 5.1 commands.yml（人可手改）

```yaml
commands:
  - id: top-mem                # 唯一即可
    title: 查看整机内存          # 必填
    cmd: hdc shell "top -n 1 | head -30"   # 必填
    groups: [性能采集, 常用]      # 多分组，分组名即主键；项目分组写完整路径
    note: 看 Pss 前先确认整机水位  # 可选
    tags: [内存, top]           # 可选，参与搜索
    danger: false               # true = 卡片显示「危险」徽标；运行通道恢复时需二次确认（终端内不带回车，§4.3）
```

### 5.2 state.json（机器维护）

```json
{
  "pinnedGroups": ["常用", "性能采集"],
  "lastUsedViewId": "group:性能采集",
  "viewLastUsedAt": { "group:D:\\work\\car_media": 1755760000000 }
}
```

### 5.3 存储位置与同步

- 目录：`%USERPROFILE%\.dsh\profiles\web\cmd-pad\`（可用 `config.dataDir` 覆盖）；
- 写入：原子写（临时文件 + rename），commands.yml 写前自动备份 `.bak`；
- 多标签页：last-writer-wins + 面板激活时拉取最新（主形态靠 `onActivate`，降级形态靠抽屉每次打开）；
- 手改 YAML：面板激活时自动生效（同一机制）。

---

## 6. 技术架构

### 6.1 bundle 结构

```
dsh-cmd-pad/
├── package.json       # dsh.bundle.patch + dsh.client 声明；peer 含 dsh-better-sidebar(optional)
├── cordis.patch.yml   # - insert: [{ id: cmd-pad, name: 'dsh-cmd-pad' }]
├── src/
│   ├── index.js       # host 半：/cmd-pad/api 路由 + YAML 文件读写（零运行时依赖）
│   └── client.js      # client 半：Tab 注册 + 降级抽屉（手写 wire format，零构建）
└── README.md
```

- **host 半**（不变）：`ctx.webServer.register` 前缀路由（GET/PUT library、PUT state），串行写队列，Host 头信任围栏；YAML 优先动态加载 `yaml` 包、缺省回退内置迷你读写器（对应官方规范 R5-3：link 安装回插件真实路径解析依赖）；
- **client 半打包格式**：`window.__ModuleLoader__.load({ id: 'dsh-cmd-pad', factory })` 手写 wire format——执行仅注册工厂，副作用全部收在 factory 闭包内；id 必须等于包名（宿主按 `/plugins/<包名>/client.js` 供应该文件）；

### 6.2 React 桥接（主形态，零构建的关键）

`registerTab` 的 `component` 契约是 `(props) => ReactNode`，而 cmd-pad 坚持零构建纯 DOM。解法：模块加载器的白名单模块表**包含 react**——factory 内 `const React = require('react')`，写一个约 20 行的桥接组件：

```
CmdPadTab(props):
  div ref 容器 → useEffect 内把现有 DOM 面板挂进容器
  依赖 [scope.sessionId, scope.cwd, visible]：scope 变化 → 卸载重挂
  visible=false → 暂停刷新（性能门）
```

不产生 JSX、不引入构建链，内容区代码两种形态 100% 复用（渲染目标容器不同而已）。

### 6.3 API

| 路由 | 方法 | 说明 |
|---|---|---|
| `/cmd-pad/api/library?sessionId=` | GET | 返回 `{ library, state, cwd, mtime }` |
| `/cmd-pad/api/library` | PUT | 全量保存命令库（last-writer-wins） |
| `/cmd-pad/api/state` | PUT | 增量保存面板状态 |
| `/sidebar/ws/terminal?sessionId&tab&cwd` | WS | （better-sidebar 路由）终端直写通道，见 §4.2 |

---

## 7. 功能清单与优先级

| # | 功能 | 状态 |
|---|---|---|
| F1 | 入口与面板（v0.1 抽屉；v0.2 起 better-sidebar Tab 主形态 + 抽屉降级） | ✅ v0.1 / ✅ v0.2（T06 落地，2026-08-23） |
| F2 | 分组浏览（全部/当前项目/常驻/▸更多；打开即定位上次使用分组——呈现定稿见调整记录 #17） | ✅ v0.1 |
| F3 | 一键复制（按钮 + 命令块点击） | ✅ v0.1 |
| F4 | 运行（主形态：新开专用终端 + 终端直写；降级形态无运行）+ 危险确认 | ✅ v0.2（T07 落地，2026-08-23） |
| F5 | 全局搜索（命中高亮、命中数、Esc 清空） | ✅ v0.1 |
| F6 | 添加/编辑（分组多选、当场新建分组、多行 && 合并） | ✅ v0.1 |
| F7 | 删除（语境语义 + 确认 + 撤销） | ✅ v0.1 |
| F8 | 右键菜单（分组：常驻/重命名/删除；卡片：运行[主形态]/复制/编辑/删除） | ✅ v0.1 / ✅ v0.2 |
| F9 | 项目识别与会话跟随（v0.2 主形态改由 scope.cwd 供给） | ✅ v0.1 / ✅ v0.2（T06 落地，2026-08-23） |
| F10 | 主题跟随（--dsw-alias-* 令牌） | ✅ v0.1 |
| F11 | 终端直写运行通道（WS 附加协议，v0.13.1 实机实证） | ✅ v0.2（T07 落地，2026-08-23） |
| F12 | 降级抽屉去蒙层（非模态化） | ✅ v0.2（T01 起即非模态无蒙层；T08 核对销项，2026-08-23） |
| F13 | 图标收敛（去 emoji/彩色，仅 2 处单色功能图形） | ✅ v0.2（每形态恰 2 处单色 SVG，视觉规范 §6 静态检查锁定；T08 核对销项，2026-08-23） |
| F14 | 导入/导出命令库 | P1（另行立项，见 TASK.md「后续立项」） |
| F15 | 拖拽排序 | P1（另行立项，见 TASK.md「后续立项」） |
| F16 | 终端选中即存 | P1（另行立项，见 TASK.md「后续立项」） |
| F17 | 键盘 ↑↓+Enter | P2（另行立项，见 TASK.md「后续立项」） |
| F18 | 命令参数占位符 | P2（另行立项，见 TASK.md「后续立项」） |
| F19 | 入驻 better-sidebar 推荐插件目录（提 PR + 打 topic） | P2（另行立项，见 TASK.md「后续立项」） |

> **T08 销项（2026-08-23）**：F1–F13 全部 ✅（v0.2 项经 T06/T07 落地 + T08 核对销项；全量回归 **138 项全过**，见 TASK.md T08 完成证据）。F14–F19（P1/P2）按实际价值**另行立项**，不在当前落地路线图内，立项清单与价值评估见 `TASK.md`「后续立项（T08）」。

## 8. 待确认项

1. **终端双 WS 附加实机验证**（F11 前提）：向终端 pty 附加第二路写入连接，验证输出回放与输入落位。
   → **已闭环（T07，2026-08-23）**：对实际安装 **better-sidebar v0.13.1** 实机实证 **13/13 通过**
   （`test/t07-ws-probe.mjs`）：A/B 双 WS 附加同一 pty、B 写入 A 可见、B 附加不重建 pty、
   双工对称、危险命令不带 `\r` 停在提示符、bare drop 后 pty 走 grace 存活。并**端到端验证**
   （cmd-pad「运行」→ 新终端 → 写文件副作用出现）。结论：**协议可用，按 §4.2 落地**；
   每次升级 better-sidebar 后回归（接入规范 §3）。
2. 终端复用策略：当前设计"复用最近终端 Tab，无则新开"。
   → **已定稿（T07，2026-08-23）**：用户决策 **"每次运行新开专用终端 Tab"**（避免向活跃
   终端写入与用户输入互相干扰；实证双 WS 附加本身互不干扰，但用户侧视图零干扰更稳妥）。

已闭环：面板挂点（registerTab / 自挂 portal 双路径）、项目识别（scope.cwd + session header.cwd）、主题令牌、client 挂载约定（dsh.client + exports["./client"] + `__ModuleLoader__` wire format）、better-sidebar 软依赖探测模式（生态先例 dsh-sentinel）。

## 9. 实现状态与版本历史

- **v0.1**（2026-08-21）：独立抽屉形态完整落地（F1–F3、F5–F10；F4 运行曾落地后按用户决策移除，见头部现状注记），纯 JS 无构建，本地冒烟通过；
- **v0.2**（2026-08-23，T06–T08 落地）：双形态重构（Tab 主形态 + 非模态抽屉降级）、终端直写运行通道（新开专用终端 + WS 直写 + 危险双人工确认 + 降级复制）、图标收敛、badge 移除；**T08 收尾：F1–F13 全部销项 + 138 项全量回归 + 真机明暗切换实证**；**里程碑 v0.2.0 tag 决策见 TASK.md 调整记录 #25（未打，用户未要求）**；
- 安装：`dsh plugin --profile web add <目录>`，装完重启 `dsh web` + 硬刷新浏览器。
